services:
  net_runner:
    image: alpine
    container_name: net_runner
    command:
      [ "sh", "-c", "echo 'This is a placeholder service'" ]
    networks:
      - app-network

networks:
  app-network:
    name: app-network
    external: false